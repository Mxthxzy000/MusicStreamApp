name: flashback_stream
description: Flashback Stream
publish_to: 'none'

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.6
  provider: ^6.1.2
  go_router: ^14.2.0
  cached_network_image: ^3.3.1
  audioplayers: ^6.0.0
  google_fonts: ^6.2.1
  shimmer: ^3.0.0
  flutter_dotenv: ^5.1.0

flutter:
  uses-material-design: true
  assets:
    - .env